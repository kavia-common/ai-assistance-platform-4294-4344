# Build-time configuration for the React frontend
# Provide the backend base URL used for API calls.
# If omitted and the app runs at http://localhost:3000, it defaults to http://localhost:3001
REACT_APP_API_BASE=http://localhost:3001

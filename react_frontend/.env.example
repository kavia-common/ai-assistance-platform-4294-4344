# Example environment variables for React frontend
# PUBLIC_INTERFACE
# REACT_APP_API_BASE defines the backend API base (protocol + host + port), e.g.:
# http://localhost:3001
REACT_APP_API_BASE=http://localhost:3001
